CREATE TABLE #TEMP
(
    ID INT IDENTITY(1,1),
    TOTAL INT
)
INSERT INTO #TEMP(TOTAL)
SELECT MONTHS * SALARY FROM EMPLOYEE

SELECT MAX(TOTAL), COUNT(ID) FROM #TEMP WHERE TOTAL = (SELECT TOP 1 TOTAL FROM #TEMP ORDER BY TOTAL DESC) 
DROP TABLE #TEMP