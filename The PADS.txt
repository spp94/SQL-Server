SELECT NAME + '(' + LEFT(OCCUPATION,1) +')' FROM OCCUPATIONS ORDER BY NAME 
CREATE TABLE #TEMP
(
    ID INT IDENTITY(1,1),
    OCCUPATION VARCHAR(MAX),
    COUNTER INT
)
INSERT INTO #TEMP(OCCUPATION,COUNTER)
SELECT OCCUPATION,COUNT(OCCUPATION) FROM
OCCUPATIONS
GROUP BY OCCUPATION

SELECT 'There are a total of '+CAST(COUNTER AS VARCHAR(MAX))+' '+LOWER(occupation)+'s.'
FROM #TEMP
ORDER BY COUNTER, OCCUPATION
DROP TABLE #TEMP
